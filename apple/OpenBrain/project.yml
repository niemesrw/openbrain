name: OpenBrain
options:
  bundleIdPrefix: com.blanxlait
  xcodeVersion: "16.0"
  generateEmptyDirectories: true

packages:
  OpenBrainKit:
    path: ../shared/OpenBrainKit

settings:
  base:
    SWIFT_VERSION: "5.10"
    DEVELOPMENT_TEAM: GCRLV5UC4Q
    CODE_SIGN_STYLE: Automatic

targets:
  OpenBrain-iOS:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: iOS
        resourceFiles:
          - iOS/PrivacyInfo.xcprivacy
    dependencies:
      - package: OpenBrainKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.blanxlait.openbrain.ios
        PRODUCT_NAME: OpenBrain
        CODE_SIGN_ENTITLEMENTS: iOS/OpenBrain.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    info:
      path: iOS/Info.plist
      properties:
        CFBundleName: Open Brain
        CFBundleDisplayName: Open Brain
        CFBundleIdentifier: com.blanxlait.openbrain.ios
        CFBundleVersion: "1"
        CFBundleShortVersionString: "1.0.0"
        CFBundlePackageType: APPL
        CFBundleExecutable: OpenBrain
        MinimumOSVersion: "17.0"
        CFBundleURLTypes:
          - CFBundleURLSchemes:
              - openbrain
            CFBundleURLName: com.blanxlait.openbrain.oauth
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
        ITSAppUsesNonExemptEncryption: false
        UILaunchScreen: {}

  OpenBrain-macOS:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: macOS
        resourceFiles:
          - macOS/PrivacyInfo.xcprivacy
    dependencies:
      - package: OpenBrainKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.blanxlait.openbrain.macos
        PRODUCT_NAME: OpenBrain
        CODE_SIGN_ENTITLEMENTS: macOS/OpenBrain.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        MACOSX_DEPLOYMENT_TARGET: "14.0"
    info:
      path: macOS/Info.plist
      properties:
        CFBundleName: Open Brain
        CFBundleDisplayName: Open Brain
        CFBundleIdentifier: com.blanxlait.openbrain.macos
        CFBundleVersion: "1"
        CFBundleShortVersionString: "1.0.0"
        CFBundlePackageType: APPL
        CFBundleExecutable: OpenBrain
        LSMinimumSystemVersion: "14.0"
        CFBundleURLTypes:
          - CFBundleURLSchemes:
              - openbrain
            CFBundleURLName: com.blanxlait.openbrain.oauth
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
        ITSAppUsesNonExemptEncryption: false
        NSPrincipalClass: NSApplication
